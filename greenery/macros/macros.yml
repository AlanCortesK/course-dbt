version: 2

macros:
  - name: get_distinct_values
    description: Get unique values from a column in a model 
    docs:
      show: true
    arguments:
      - name: model
        type: <string>
        description: Name of the model
      - name: column_name
        type: <string>
        description: Column from wich unique values will be retrieved